// import React from "react";

// const ButtonSelect = () => {
//   if (
//     minDate?.getTime() > new Date(currentYaer, i + 1) ||
//     maxDate?.getTime() < new Date(currentYaer, i + 1)
//   ) {
//     return (
//       <button key={uuid()} onClick={() => handleSetCurrent(i)} disabled={true}>
//         {month}
//       </button>
//     );
//   }
//   return <div>ButtonSelect</div>;
// };

// export default ButtonSelect;
